buildscript {
  repositories {
    jcenter()
  }
  dependencies {
    classpath 'ca.cutterslade.gradle:gradle-dependency-analyze:1.2.2'
  }
}

allprojects {
  repositories {
    jcenter()
  }

  apply plugin: 'ca.cutterslade.analyze'
}

subprojects {
  apply plugin: 'java'
}
